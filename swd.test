[Test]
ExecStart=sudo /opt/openocd/bin/openocd -f interface/raspberrypi2-native.cfg -c "transport select swd" -f target/kx.cfg -c "init" -c "kx.cpu configure -rtos ChibiOS" -c 'reset halt'
Name=Run OpenOCD
Name[zh]=运行 OpenOCD
Description=Run the OpenOCD debugger in the background.
Description[zh]=在后台运行OpenOCD调试器。
Timeout=10
Type=daemon
